import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Plan a Trip — AIVIBLE" };

export default function PlanPage() {
  return <ComingSoon title="Plan a Trip" />;
}
